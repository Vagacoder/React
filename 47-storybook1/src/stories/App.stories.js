import React from 'react';
import App from '../App';

export default { title: 'React default App' };

export const app = () => {
    return <App />;
};

app.story = { name: "React App Default Page", };